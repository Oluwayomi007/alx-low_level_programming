#ifndef MAIN_H
#define MAIN.H

#include <stdio.h>

void positive_or_negetive(int i);
int largest_number(int a, int b, int c);
void print_reaining_days(int month, int day, int year);
int convert_day(int, int);
 
#endif /* MAIN_H * /
